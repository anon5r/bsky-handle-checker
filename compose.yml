services:
  app:
    build:
      context: .
    image: bsky-handle-checker:latest
    container_name: bluesky-checker
    volumes:
      - .:/app
      - ./data:/app/data
    environment:
      - NODE_ENV=production
